export type NavigationItem = {
  label: string;
  href: string;
};

export type ProjectCard = {
  title: string;
  href: string;
  summary: string;
  meta: string;
};

export type CaseStudy = {
  label: string;
  title: string;
  body: string;
};

export type ExperienceItem = {
  period: string;
  company: string;
  summary: string;
};
